namespace SimplCommerce.Infrastructure.Caching
{
    /// <summary>
    /// Cacheable entity (EF doesn't support 2-level caching now)
    /// </summary>
    public class IEntityForCaching
    {
    }
}
